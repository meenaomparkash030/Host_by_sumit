[build]
  command = "pip install -r requirements.txt"
  publish = "."

[functions]
  directory = "functions"

[[redirects]]
  from = "/*"
  to = "/app.py"
  status = 200

[build.environment]
  PYTHON_VERSION = "3.11"
  PLATFORM = "netlify"